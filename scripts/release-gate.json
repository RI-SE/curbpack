{
  "schema": "curbpack-release-gate:1",
  "version": "v0.5.3",
  "assets_verified": true,
  "checksums_verified": true,
  "install_smoke_verified": true,
  "scan_write_free_verified": true,
  "notes": "CLI install pin for public copy. Public pin updates require install-manifest.json default_version == this version and all *_verified flags true. Action pin remains @v0.5.2 until human pin-bump."
}
