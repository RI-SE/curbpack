#!/usr/bin/env bash
# DEPRECATED — dual-remote sync removed when RI-SE/curbpack became the single source of truth.
set -euo pipefail
echo "Deprecated: ./scripts/curb-sync.sh is no longer used."
echo "Canonical repo: https://github.com/RI-SE/curbpack — push via branch + PR only."
echo "Never force-push to main."
exit 2
