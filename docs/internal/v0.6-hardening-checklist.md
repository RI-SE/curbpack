# v0.6 hardening checklist

Release-grade guardrail evidence before advancing the Action/examples pin beyond `@v0.5.2`.

## SME tabletop (required)

Run these scenarios locally or in a paired review session. Record outcomes in the PR or release notes.

| Scenario | Expected safe behavior | Pass | Evidence |
|----------|------------------------|------|----------|
| Forged Git Note (`user_touch=ssh-agent-signed`, no valid sig) | Buyer surfaces stay **UNSIGNED**; AttestDisplay never green from string alone | ☐ | |
| Tampered `latest_failure.json` (green cache, red tree) | ContextPack re-validates; export not fake-green | ☐ | [`internal/exportx/context_pack_adversarial_test.go`](../../internal/exportx/context_pack_adversarial_test.go) |
| Cross-pack stale cache (`pack_id` mismatch) | ContextPack ignores cache; uses requested packs | ☐ | [`internal/exportx/context_pack_adversarial_test.go`](../../internal/exportx/context_pack_adversarial_test.go) |
| HEAD moved after cache write | Cache rejected unless `expected_parent_commit_sha` matches HEAD | ☐ | [`internal/exportx/context_pack_adversarial_test.go`](../../internal/exportx/context_pack_adversarial_test.go) |
| Symlink escape under repo root | SafeJoin / pathjail refuses | ☐ | [`internal/pathjail/pathjail_test.go`](../../internal/pathjail/pathjail_test.go) |
| `.Git/` path (case variant) | Refused by path jail | ☐ | [`internal/pathjail/pathjail_test.go`](../../internal/pathjail/pathjail_test.go) |
| `curbpack check --json --heal` | stdout is parse-safe JSON only (no markdown hints) | ☐ | [`internal/cli/check_json_heal_test.go`](../../internal/cli/check_json_heal_test.go) |
| Single canonical repo | `RI-SE/curbpack` `main` matches release tag | ☐ | |
| Attest without `--allow-dirty` on dirty tree | OCC conflict; no silent bind | ☐ | [`internal/attest/adversarial_test.go`](../../internal/attest/adversarial_test.go) |

### Tabletop evidence template

Copy into your tabletop GitHub issue or PR comment:

```markdown
## Tabletop row N — <scenario name>
- **Command:** `<exact command>`
- **Expected:** <one line>
- **Observed:** <one line>
- **Pass/fail:** pass | fail
- **Commit/SHA:** `<main SHA>`
- **Notes:** <optional>
```

## Automated gates (required)

Checked on `main` @ `e2f14da`:

- [x] `go test ./...` green on Linux, macOS, Windows CI matrix — merge PRs #66, #69, #70
- [x] `go run ./cmd/curbpack check` green in this repo (dogfood)
- [x] Golden tests pass (`internal/exportx/golden_test.go`); update with `-update` only when intentional
- [x] Fuzz targets (`FuzzSafeJoin`, `FuzzTextForbidRegex`, `FuzzParseHPURLFragment`) pass CI short fuzz
- [x] `redteam-pilot` required check green on merge PR
- [x] Claim-safety script green (`scripts/claim-safety.sh`)

## Trust-surface freeze (required)

- [ ] No new pack ids without human pathway confirm
- [ ] Capsule `schema_version` bump documented when `state_hash` field order changes
- [ ] No weakening of cite-or-refuse / confirm-prose grounding
- [ ] MCP remains propose-only (no confirm/attest tools)

## Release promotion criteria

Advance pin/tag only when **all** tabletop rows and automated gates above are checked, with:

1. Link to green CI run on the release commit
2. Link to merged PR with adversarial test coverage summary
3. Human attestation that release smoke passed post-merge on RI-SE/curbpack

## Deferred / human-only blockers

- **Branch protection context names** — if required checks rename, update source repo ruleset to match `ci` job names (`test`, `redteam-pilot`, etc.)

_Not a conformity assessment. This checklist prepares evidence for human review._
